// Complex resolver combinations of recursion, fallback, and scope

describe('complex variable resolution', () => {
  it('should choose correct fallback in nested chains', () => {
    // TODO
  });
});
