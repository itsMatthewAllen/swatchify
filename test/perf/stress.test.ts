// Performance/stress tests - run separately to avoid slowing normal test runs

describe('stress tests (optional)', () => {
  it('should resolve a large number of variables quickly', () => {
    // TODO: generate many variables and test resolution time
  });
});
